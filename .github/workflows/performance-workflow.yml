name: Performance Test
on:
  pull_request:
    branches:
    - 'dbartol/perf'
jobs:
  new:
    name: Measure performance of new code
    runs-on:
    - windows-latest
    steps:
    - uses: actions/checkout@v1
